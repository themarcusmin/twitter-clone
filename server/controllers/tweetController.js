const Tweet = require("../model/Tweet");

// Handle POST request on creating tweet
module.exports.tweet_post = async (req, res) => {
    // const { tweeter_id, fullname, username, content } = req.body;
    console.log(req.body)
    // try {
    //     const tweet = await Tweet.create
    // } catch (err) {
    //     console.log(err.message)
    // }
}